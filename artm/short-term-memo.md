map
    in_bounds
    can_enter
    try_idx -> Option<usize>
player has own module
    and add to prelude
    struct
        position (bracket-lib's Point)
        render as @
        update
            reacts to motion keys
            uses can_enter
